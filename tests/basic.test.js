import { describe, it, expect } from 'vitest';

describe('Create Ignite', () => {
  it('should have a valid package name', () => {
    expect('create-ignite').toBe('create-ignite');
  });

  it('should be a valid CLI tool', () => {
    expect(true).toBe(true);
  });
});
